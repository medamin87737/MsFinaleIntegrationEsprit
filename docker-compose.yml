# ---------------------------------------------------------------------------
# Stack COMPLÈTE (grille d’évaluation) : Eureka, Config Server, API Gateway,
# les 5 microservices Spring, MSNotes (Nest), Keycloak, MongoDB, RabbitMQ.
#
#   cd MS
#   docker compose up -d --build
#
# Infra seule (Keycloak + Mongo + Rabbit, MS lancés depuis l’IDE) :
#   docker compose -f docker-compose.infra.yml up -d
#
# Équivalent explicite : docker compose -f docker-compose.full-stack.yml …
# ---------------------------------------------------------------------------

include:
  - docker-compose.full-stack.yml
