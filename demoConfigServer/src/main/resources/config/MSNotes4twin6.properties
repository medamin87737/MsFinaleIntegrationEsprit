welcome.message=Welcome to MSNotes4twin6 MS
server.port=8088
# MongoDB (notes) — aligné sur MSNotes4twin6/.env (MONGODB_URI)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://mongodb:27017/ms_notes}

keycloak.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/school-realm}
keycloak.jwt-audience=${KEYCLOAK_JWT_AUDIENCE:account}

# Appels serveur → gateway (création d’inscription implicite à la saisie de note : vérif. élève / matière même classe)
GATEWAY_BASE_URL=${GATEWAY_BASE_URL:http://api-gateway:8080}
